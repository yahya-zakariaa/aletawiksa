/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["media.zid.store", "store.storeimages.cdn-apple.com"],
    // أضف الدومين هنا
  },
};

export default nextConfig;
